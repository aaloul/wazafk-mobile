import 'package:flutter/material.dart';

class WhereLoggedItem extends StatelessWidget {
  const WhereLoggedItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
